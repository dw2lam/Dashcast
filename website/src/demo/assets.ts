const base = '/';

export const ASSETS = {
  /** The extended display gets its own wallpaper; mirror shows the Mac's own screen. */
  wallpaper: { extend: base + 'demo/wallpaper.webp', mirror: base + 'shots/wallpaper.jpg' },
  clip: { webm: base + 'demo/clip.webm', mp4: base + 'demo/clip.mp4', poster: base + 'demo/clip-poster.jpg' },
};
