const base = '/';

export const ASSETS = {
  /** The brand wallpaper, shared with the Mac app showcase (one palette across the site). */
  wallpaper: { extend: base + 'shots/wallpaper.jpg', mirror: base + 'shots/wallpaper.jpg' },
  clip: { webm: base + 'demo/clip.webm', mp4: base + 'demo/clip.mp4', poster: base + 'demo/clip-poster.jpg' },
};
