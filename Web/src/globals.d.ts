// Injected by build.mjs (esbuild `define`): minified sources of the video Worker
// and the AudioWorklet, loaded at runtime through Blob URLs so the page stays one file.
declare const __WORKER_SRC__: string;
declare const __WORKLET_SRC__: string;
